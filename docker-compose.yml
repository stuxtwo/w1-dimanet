# version: '3.8' disabled this parameter, this is now obselete.

services:
  w1-dimanet:
    build:
      context: .
    container_name: w1-dimanet
    volumes:
      - .:/app
    command: bash -c "while true; do sleep 1000; done"
